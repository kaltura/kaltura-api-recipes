<h1>Cue Points</h1>
<hr></hr>
<% if result.length === 0 %>
  <i>No cue points found</i>
<% end %>
<% result.each_index do | index | cuePoint = result.fetch(index) %>
  <p>
  <%= cue_point.cue_point_type %> starting at <%= cue_point.start_time %> milliseconds
  </p>
<% end %>
<%= render "add_ad_cue_point.html.erb" %>
<hr></hr>