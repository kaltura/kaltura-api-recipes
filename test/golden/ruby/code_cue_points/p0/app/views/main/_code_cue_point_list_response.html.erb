<h1>Cue Points</h1>
<hr></hr>
<% if result.length === 0 %>
  <i>No cue points found</i>
<% end %>
<% result.each_index do | index | cuePoint = result.fetch(index) %>
  <h3><%= cue_point.code %></h3>
  <p><%= cue_point.start_time %>ms - <%= cue_point.end_time %>ms</p>
  <p><%= cue_point.description %></p>
<% end %>
<%= render "add_code_cue_point.html.erb" %>
<hr></hr>